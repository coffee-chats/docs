---
title: Contact tools
description: Tools that read your contacts, their notes and tags, and your network counts, and replace the notes and the tags on one contact.
---

These tools read your contacts, their notes and tags, and your network counts, and replace the notes and the tags on one contact. They need the permissions `contacts:read` and `contacts:write`, which you grant on the consent screen when you connect.

This page is generated from the connector's tool catalogue. It describes 8 tools. Every tool reads or writes only your own account.

## Permissions this page needs

- `contacts:read`: Read your contacts and network information
- `contacts:write`: Update the notes and tags on your contacts

The [permissions page](/mcp/scopes) lists every permission you can grant.

## `contacts.list`

List every authenticated student contact, including DO_NOT_CONTACT records, with measured pagination.

Permissions: This tool needs `contacts:read`, which the consent screen describes as "Read your contacts and network information".

Risk: `read`. This tool only reads your own data. It never changes anything and it never asks for your approval.

### Input

| Property | Type | Required | Bounds |
| --- | --- | --- | --- |
| `cursor` | string | no | 1 to 512 characters; pattern `^[A-Za-z0-9_-]+$` |
| `limit` | integer | no | from 1 to 20 |
| `origin` | string | no | one of `CAMPAIGN`, `MANUAL_ENTRY`, `LINKEDIN_IMPORT`, `CSV_IMPORT`, `BOOKING`, `RESERVATION` |
| `status` | string | no | one of `NEW`, `NEEDS_REVIEW`, `APPROVED`, `SENDING`, `CONTACTED`, `REPLIED`, `SCHEDULED`, `COMPLETED`, `DO_NOT_CONTACT` |

### Example call

```json
{
  "limit": 10,
  "status": "APPROVED"
}
```

### Result

A result is never larger than 16,384 bytes.
If the stored value does not fit, the tool returns a measured fragment and a cursor for the next one.

This tool's result takes one of these 2 forms, and one call returns exactly one of them. The field named beside each form below says which one you are looking at.

Shape 1 carries these top-level fields:

```json
[
  "contacts",
  "nextCursor"
]
```

Shape 2 carries these top-level fields:

```json
[
  "contacts",
  "fragment",
  "nextCursor"
]
```

### Errors

A failed call returns one protocol error whose text is exactly `Tool call failed.` There is no error code, because every read failure is reported the same way.

## `contacts.get`

Read one authenticated student contact by id or its owner-scoped generated slug.

Permissions: This tool needs `contacts:read`, which the consent screen describes as "Read your contacts and network information".

Risk: `read`. This tool only reads your own data. It never changes anything and it never asks for your approval.

### Input

The agent sends exactly one of these shapes and never a mixture of them. Each shape refuses any property it does not name.

#### Shape 1, sending `id`

| Property | Type | Required | Bounds |
| --- | --- | --- | --- |
| `cursor` | string | no | 1 to 512 characters; pattern `^[A-Za-z0-9_-]+$` |
| `id` | string | yes | pattern `^[a-f0-9]{24}$` |

##### Example call

```json
{
  "id": "0123456789abcdef01234567"
}
```

#### Shape 2, sending `slug`

| Property | Type | Required | Bounds |
| --- | --- | --- | --- |
| `cursor` | string | no | 1 to 512 characters; pattern `^[A-Za-z0-9_-]+$` |
| `slug` | string | yes | none stated |

##### Example call

```json
{
  "slug": "example-contact-0001"
}
```

### Result

A result is never larger than 16,384 bytes.
If the stored value does not fit, the tool returns a measured fragment and a cursor for the next one.

This tool's result takes one of these 2 forms, and one call returns exactly one of them. The field named beside each form below says which one you are looking at.

Shape 1 carries these top-level fields:

```json
[
  "contact"
]
```

Shape 2 carries these top-level fields:

```json
[
  "contact",
  "fragment",
  "nextCursor"
]
```

### Errors

A failed call returns one protocol error whose text is exactly `Tool call failed.` There is no error code, because every read failure is reported the same way.

## `contacts.notes.get`

Read one authenticated student contact note in measured fragments.

Permissions: This tool needs `contacts:read`, which the consent screen describes as "Read your contacts and network information".

Risk: `read`. This tool only reads your own data. It never changes anything and it never asks for your approval.

### Input

| Property | Type | Required | Bounds |
| --- | --- | --- | --- |
| `cursor` | string | no | 1 to 512 characters; pattern `^[A-Za-z0-9_-]+$` |
| `id` | string | yes | pattern `^[a-f0-9]{24}$` |

### Example call

```json
{
  "id": "0123456789abcdef01234567"
}
```

### Result

A result is never larger than 16,384 bytes.
If the stored value does not fit, the tool returns a measured fragment and a cursor for the next one.

A result carries these top-level fields:

```json
[
  "fragment",
  "nextCursor"
]
```

### Errors

A failed call returns one protocol error whose text is exactly `Tool call failed.` There is no error code, because every read failure is reported the same way.

## `contacts.tags.get`

Read every authenticated student contact tag in measured fragments.

Permissions: This tool needs `contacts:read`, which the consent screen describes as "Read your contacts and network information".

Risk: `read`. This tool only reads your own data. It never changes anything and it never asks for your approval.

### Input

| Property | Type | Required | Bounds |
| --- | --- | --- | --- |
| `cursor` | string | no | 1 to 512 characters; pattern `^[A-Za-z0-9_-]+$` |
| `id` | string | yes | pattern `^[a-f0-9]{24}$` |

### Example call

```json
{
  "id": "0123456789abcdef01234567"
}
```

### Result

A result is never larger than 16,384 bytes.
If the stored value does not fit, the tool returns a measured fragment and a cursor for the next one.

A result carries these top-level fields:

```json
[
  "fragment",
  "nextCursor"
]
```

### Errors

A failed call returns one protocol error whose text is exactly `Tool call failed.` There is no error code, because every read failure is reported the same way.

## `contacts.stats.get`

Count every authenticated student contact, including DO_NOT_CONTACT records, by status and due follow-up state.

Permissions: This tool needs `contacts:read`, which the consent screen describes as "Read your contacts and network information".

Risk: `read`. This tool only reads your own data. It never changes anything and it never asks for your approval.

### Input

This tool takes no input.

### Example call

```json
{}
```

### Result

A result is never larger than 16,384 bytes.

A result carries these top-level fields:

```json
[
  "byStatus",
  "followUpsDue",
  "total"
]
```

### Errors

A failed call returns one protocol error whose text is exactly `Tool call failed.` There is no error code, because every read failure is reported the same way.

## `contacts.network.stats.get`

Count the authenticated student network population after excluding DO_NOT_CONTACT records.

Permissions: This tool needs `contacts:read`, which the consent screen describes as "Read your contacts and network information".

Risk: `read`. This tool only reads your own data. It never changes anything and it never asks for your approval.

### Input

This tool takes no input.

### Example call

```json
{}
```

### Result

A result is never larger than 16,384 bytes.

A result carries these top-level fields:

```json
[
  "contacted",
  "new",
  "replied",
  "scheduled",
  "total",
  "unspecified"
]
```

### Errors

A failed call returns one protocol error whose text is exactly `Tool call failed.` There is no error code, because every read failure is reported the same way.

## `contacts.notes.update`

What this changes: Replace the private notes on one of the authenticated student's own contacts, with durable idempotency.

The whole note is replaced rather than appended to, at most 8000 characters, and the write fails without changing anything if anybody edited that contact's notes first.

Permissions: This tool needs `contacts:write`, which the consent screen describes as "Update the notes and tags on your contacts".

Risk: `safe_write`. This tool changes one of your own settings as soon as the agent calls it. It does not ask you to approve first, because the change affects only your own account and you can set it back yourself.

### Input

| Property | Type | Required | Bounds |
| --- | --- | --- | --- |
| `contactId` | string | yes | pattern `^[a-f0-9]{24}$` |
| `idempotencyKey` | string | yes | 16 to 128 characters |
| `notes` | string | yes | none stated |

### Example call

```json
{
  "contactId": "0123456789abcdef01234567",
  "idempotencyKey": "example-idempotency-key-0006",
  "notes": "Met at the spring fintech mixer. Follow up in April about the referral."
}
```

### Result

A result is never larger than 4,096 bytes.

This tool's result takes one of these 4 forms, and one call returns exactly one of them. The field named beside each form below says which one you are looking at.

Shape 1, where `tag` is `completed`, carries these top-level fields:

```json
[
  "operationId",
  "operationStatus",
  "result",
  "scenario",
  "tag"
]
```

Shape 2, where `tag` is `accepted`, carries these top-level fields:

```json
[
  "operationId",
  "operationStatus",
  "scenario",
  "tag"
]
```

Shape 3, where `status` is `failed`, carries these top-level fields:

```json
[
  "error",
  "operationId",
  "scenario",
  "status"
]
```

Shape 4, where `status` is `uncertain`, carries these top-level fields:

```json
[
  "error",
  "operationId",
  "scenario",
  "status"
]
```

### The result may carry a retry hint

A call can be accepted and still ask the agent to come back later. When that happens the result is an accepted result and not a failure, and it carries a hint beside it holding one of these codes and a delay in milliseconds. Nothing has gone wrong and nothing has been written yet.

| Code | What it means |
| --- | --- |
| `PERMIT_BACKPRESSURE` | Too much work is already running on this connection. The agent should wait for the stated delay and call the tool again. |
| `PERMIT_CONTENTION` | Another call was holding the same slot. The agent should wait for the stated delay and call the tool again. |

### Errors

A call that does not succeed carries one of these codes.

| Code | What it means |
| --- | --- |
| `ACTION_DENIED` | Coffee Chats would not let this connection make the change, so nothing was written. The connection may have been revoked, or it may no longer hold the permission this tool needs. |
| `ACTION_EXPIRED` | Coffee Chats did not finish the change inside the short window it allows itself, and stopped rather than write late. Nothing was written. Ask the agent to make the change again. |
| `NOT_APPLIED` | Coffee Chats stopped before writing anything. Your data is exactly as it was. |
| `OUTCOME_UNKNOWN` | Coffee Chats cannot prove whether the change was written and will not guess. Open your Coffee Chats page and look before you decide what to do. Do not ask the agent to retry with a new key, because that could apply the change twice. |
| `SERVICE_UNAVAILABLE` | Coffee Chats could not do the work just now and wrote nothing. Try again later. |
| `VERSION_CONFLICT` | Your stored preference changed after the agent read it, so nothing was written. Ask the agent to read it again and make the change from what is there now. |

## `contacts.tags.update`

What this changes: Replace the tags on one of the authenticated student's own contacts, with durable idempotency.

The whole list is replaced rather than merged, at most 10 tags of 60 characters each, and the write fails without changing anything if anybody edited that contact's tags first.

Permissions: This tool needs `contacts:write`, which the consent screen describes as "Update the notes and tags on your contacts".

Risk: `safe_write`. This tool changes one of your own settings as soon as the agent calls it. It does not ask you to approve first, because the change affects only your own account and you can set it back yourself.

### Input

| Property | Type | Required | Bounds |
| --- | --- | --- | --- |
| `contactId` | string | yes | pattern `^[a-f0-9]{24}$` |
| `idempotencyKey` | string | yes | 16 to 128 characters |
| `tags` | array | yes | none stated |

### Example call

```json
{
  "contactId": "0123456789abcdef01234567",
  "idempotencyKey": "example-idempotency-key-0007",
  "tags": [
    "fintech",
    "warm"
  ]
}
```

### Result

A result is never larger than 4,096 bytes.

This tool's result takes one of these 4 forms, and one call returns exactly one of them. The field named beside each form below says which one you are looking at.

Shape 1, where `tag` is `completed`, carries these top-level fields:

```json
[
  "operationId",
  "operationStatus",
  "result",
  "scenario",
  "tag"
]
```

Shape 2, where `tag` is `accepted`, carries these top-level fields:

```json
[
  "operationId",
  "operationStatus",
  "scenario",
  "tag"
]
```

Shape 3, where `status` is `failed`, carries these top-level fields:

```json
[
  "error",
  "operationId",
  "scenario",
  "status"
]
```

Shape 4, where `status` is `uncertain`, carries these top-level fields:

```json
[
  "error",
  "operationId",
  "scenario",
  "status"
]
```

### The result may carry a retry hint

A call can be accepted and still ask the agent to come back later. When that happens the result is an accepted result and not a failure, and it carries a hint beside it holding one of these codes and a delay in milliseconds. Nothing has gone wrong and nothing has been written yet.

| Code | What it means |
| --- | --- |
| `PERMIT_BACKPRESSURE` | Too much work is already running on this connection. The agent should wait for the stated delay and call the tool again. |
| `PERMIT_CONTENTION` | Another call was holding the same slot. The agent should wait for the stated delay and call the tool again. |

### Errors

A call that does not succeed carries one of these codes.

| Code | What it means |
| --- | --- |
| `ACTION_DENIED` | Coffee Chats would not let this connection make the change, so nothing was written. The connection may have been revoked, or it may no longer hold the permission this tool needs. |
| `ACTION_EXPIRED` | Coffee Chats did not finish the change inside the short window it allows itself, and stopped rather than write late. Nothing was written. Ask the agent to make the change again. |
| `NOT_APPLIED` | Coffee Chats stopped before writing anything. Your data is exactly as it was. |
| `OUTCOME_UNKNOWN` | Coffee Chats cannot prove whether the change was written and will not guess. Open your Coffee Chats page and look before you decide what to do. Do not ask the agent to retry with a new key, because that could apply the change twice. |
| `SERVICE_UNAVAILABLE` | Coffee Chats could not do the work just now and wrote nothing. Try again later. |
| `VERSION_CONFLICT` | Your stored preference changed after the agent read it, so nothing was written. Ask the agent to read it again and make the change from what is there now. |
