---
title: Billing tools
description: Tools that read your subscription, your usage, your credit history and your referral credits.
---

These tools read your subscription, your usage, your credit history and your referral credits. They need the permission `billing:read`, which you grant on the consent screen when you connect.

This page is generated from the connector's tool catalogue. It describes 5 tools. Every tool reads or writes only your own account.

## Permissions this page needs

- `billing:read`: Read your subscription, usage, and referral information

The [permissions page](/mcp/scopes) lists every permission you can grant.

## `billing.subscription.get`

Read the authenticated student's sanitized subscription and referral-credit state.

Permissions: This tool needs `billing:read`, which the consent screen describes as "Read your subscription, usage, and referral information".

Risk: `read`. This tool only reads your own data. It never changes anything and it never asks for your approval.

### Input

| Property | Type | Required | Bounds |
| --- | --- | --- | --- |
| `cursor` | string | no | 1 to 512 characters; pattern `^[A-Za-z0-9_-]+$` |

### Example call

```json
{}
```

### Result

A result is never larger than 16,384 bytes.
If the stored value does not fit, the tool returns a measured fragment and a cursor for the next one.

This tool's result takes one of these 2 forms, and one call returns exactly one of them. The field named beside each form below says which one you are looking at.

Shape 1 carries these top-level fields:

```json
[
  "avatarOverageEnabled",
  "billingInterval",
  "cancelAtPeriodEnd",
  "canceledAt",
  "chatTokensOverageEnabled",
  "currency",
  "currentPeriodEndsAt",
  "currentPeriodStartsAt",
  "freeTrialEndsAt",
  "lastSyncedAt",
  "overageEnabled",
  "pastDueSince",
  "referralCreditBalance",
  "referralCreditsExpireAt",
  "researchOverageEnabled",
  "scheduledChangeAt",
  "scheduledInterval",
  "scheduledTier",
  "status",
  "tier",
  "trialEndsAt",
  "unitAmountCents",
  "voiceOverageEnabled"
]
```

Shape 2 carries these top-level fields:

```json
[
  "fragment",
  "nextCursor"
]
```

### Errors

A failed call returns one protocol error whose text is exactly `Tool call failed.` There is no error code, because every read failure is reported the same way.

## `billing.usage.get`

Read the authenticated student's current stored usage without creating a period.

Permissions: This tool needs `billing:read`, which the consent screen describes as "Read your subscription, usage, and referral information".

Risk: `read`. This tool only reads your own data. It never changes anything and it never asks for your approval.

### Input

| Property | Type | Required | Bounds |
| --- | --- | --- | --- |
| `cursor` | string | no | 1 to 512 characters; pattern `^[A-Za-z0-9_-]+$` |

### Example call

```json
{}
```

### Result

A result is never larger than 16,384 bytes.
If the stored value does not fit, the tool returns a measured fragment and a cursor for the next one.

This tool's result takes one of these 2 forms, and one call returns exactly one of them. The field named beside each form below says which one you are looking at.

Shape 1 carries these top-level fields:

```json
[
  "aiAvatarMinutesUsed",
  "aiChatTokensUsed",
  "aiCreditsUsed",
  "aiVoiceAgentMinutesUsed",
  "avatarPracticeCreditsSettled",
  "contactsReached",
  "deepResearchUsed",
  "fastLaneTokensUsed",
  "found",
  "modelBreakdownCount",
  "overageAvatarMinutesUsed",
  "overageChatTokensUsed",
  "overageCreditsUsed",
  "overageDeepResearchUsed",
  "overageVoiceMinutesUsed",
  "periodEnd",
  "periodStart",
  "projectsCreated",
  "standardResearchUsed",
  "tier",
  "tokensMonthKey",
  "voicePracticeCreditsSettled"
]
```

Shape 2 carries these top-level fields:

```json
[
  "fragment",
  "nextCursor"
]
```

### Errors

A failed call returns one protocol error whose text is exactly `Tool call failed.` There is no error code, because every read failure is reported the same way.

## `billing.credit_history.list`

List the authenticated student's credit transactions with measured pagination.

Permissions: This tool needs `billing:read`, which the consent screen describes as "Read your subscription, usage, and referral information".

Risk: `read`. This tool only reads your own data. It never changes anything and it never asks for your approval.

### Input

| Property | Type | Required | Bounds |
| --- | --- | --- | --- |
| `cursor` | string | no | 1 to 512 characters; pattern `^[A-Za-z0-9_-]+$` |
| `limit` | integer | no | from 1 to 20 |

### Example call

```json
{
  "limit": 10
}
```

### Result

A result is never larger than 16,384 bytes.
If the stored value does not fit, the tool returns a measured fragment and a cursor for the next one.

This tool's result takes one of these 2 forms, and one call returns exactly one of them. The field named beside each form below says which one you are looking at.

Shape 1 carries these top-level fields:

```json
[
  "nextCursor",
  "transactions"
]
```

Shape 2 carries these top-level fields:

```json
[
  "fragment",
  "nextCursor",
  "transactions"
]
```

### Errors

A failed call returns one protocol error whose text is exactly `Tool call failed.` There is no error code, because every read failure is reported the same way.

## `billing.referrals.get`

Read the authenticated student's referral code, credits, and invited count.

Permissions: This tool needs `billing:read`, which the consent screen describes as "Read your subscription, usage, and referral information".

Risk: `read`. This tool only reads your own data. It never changes anything and it never asks for your approval.

### Input

| Property | Type | Required | Bounds |
| --- | --- | --- | --- |
| `cursor` | string | no | 1 to 512 characters; pattern `^[A-Za-z0-9_-]+$` |

### Example call

```json
{}
```

### Result

A result is never larger than 16,384 bytes.
If the stored value does not fit, the tool returns a measured fragment and a cursor for the next one.

This tool's result takes one of these 2 forms, and one call returns exactly one of them. The field named beside each form below says which one you are looking at.

Shape 1 carries these top-level fields:

```json
[
  "code",
  "creditBalance",
  "creditsExpireAt",
  "invitedCount",
  "lifetimeAsReferee",
  "lifetimeAsReferrer"
]
```

Shape 2 carries these top-level fields:

```json
[
  "fragment",
  "nextCursor"
]
```

### Errors

A failed call returns one protocol error whose text is exactly `Tool call failed.` There is no error code, because every read failure is reported the same way.

## `billing.limits.get`

Read the authenticated student's plan tier, entitlement source, billing period and allowances, without creating a usage period.

Permissions: This tool needs `billing:read`, which the consent screen describes as "Read your subscription, usage, and referral information".

Risk: `read`. This tool only reads your own data. It never changes anything and it never asks for your approval.

### Input

| Property | Type | Required | Bounds |
| --- | --- | --- | --- |
| `cursor` | string | no | 1 to 512 characters; pattern `^[A-Za-z0-9_-]+$` |

### Example call

```json
{}
```

### Result

A result is never larger than 16,384 bytes.
If the stored value does not fit, the tool returns a measured fragment and a cursor for the next one.

This tool's result takes one of these 2 forms, and one call returns exactly one of them. The field named beside each form below says which one you are looking at.

Shape 1 carries these top-level fields:

```json
[
  "entitlementSource",
  "freeTrial",
  "limits",
  "periodEnd",
  "periodSource",
  "periodStart",
  "readOnly",
  "selfTier",
  "tier",
  "tokenPeriodEnd"
]
```

Shape 2 carries these top-level fields:

```json
[
  "fragment",
  "nextCursor"
]
```

### Errors

A failed call returns one protocol error whose text is exactly `Tool call failed.` There is no error code, because every read failure is reported the same way.
